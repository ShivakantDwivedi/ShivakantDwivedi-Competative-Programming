#include <iostream>
using namespace std;
int main(){
    int a;
    cin>>a;
    if(a>0)
    cout<<"Number is positive number";
    else if(a<0)
    cout<<"Number  is negative number";
    else
    cout<<"Number equal to zero";
}